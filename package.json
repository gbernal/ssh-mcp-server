{
  "name": "@fangjunjie/ssh-mcp-server",
  "version": "1.0.2",
  "description": "基于 SSH 的 MCP 服务器",
  "main": "build/index.js",
  "type": "module",
  "bin": {
    "ssh-mcp-server": "build/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && chmod 755 build/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "ssh",
    "mcp",
    "server",
    "cli"
  ],
  "author": "Junki",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.7.0",
    "ssh2": "^1.16.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/ssh2": "^1.15.5",
    "typescript": "^5.8.2"
  },
  "files": [
    "build/**/*"
  ]
}
